{
  "name": "nestjs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev:db": "scripts\\start-db.sh",
    "start:dev:kafka": "scripts\\start-kafka.sh",
    "start": "lerna run start --parallel",
    "bootstrap": "lerna exec npm install",
    "dev": "lerna run dev --parallel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lerna": "^7.2.0",
    "npm-run-all": "^4.1.5"
  }
}
